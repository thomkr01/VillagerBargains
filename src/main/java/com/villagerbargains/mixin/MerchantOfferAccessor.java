package com.villagerbargains.mixin;

// MerchantOfferAccessor is no longer needed:
// we now replace offers entirely rather than mutating their final fields.
// Kept as an empty placeholder so existing mixin.json references do not break.
// Can be removed once mixin.json is cleaned up.
